# INT305 任务市场 — 毕设拆任务指南

> **核心思路**：你的毕设不是一个人能做完的。把其中 10-30% 的 ML 模块拆出来，让同学帮你做，你也帮同学做。双赢。

---

## 一、什么样的任务适合发布？

### ✅ 适合的任务

| 特征 | 举例 |
|------|------|
| **边界清晰** — 有明确的输入输出 | "输入：原始CSV → 输出：清洗后的特征矩阵" |
| **可独立完成** — 不依赖你的其他模块 | "训练一个 ResNet18 分类器，用 MedMNIST 数据集" |
| **1-2 周可完成** — 不是整个系统 | "写一个 Flask API 封装已有模型" |
| **有验收标准** — 能判断做没做完 | "准确率 ≥ 85%，代码可一键运行" |

### ❌ 不适合的任务

| 类型 | 问题 |
|------|------|
| "帮我做整个毕设" | 太大，不可独立完成 |
| "帮我写论文" | 不是 ML 技术任务 |
| "帮我调那个 bug" | 没有明确交付物 |
| "帮我跑实验" | 太模糊，无法验收 |

---

## 二、6 种任务类别

### 1. 数据预处理 Pipeline 📊
**做什么**：把原始数据变成模型能吃的格式

**典型交付物**：
- Python 预处理脚本
- 处理后的数据集（或处理流程文档）
- 数据质量报告

**验收标准示例**：
- 输入原始 CSV，输出清洗后的特征矩阵
- 缺失值处理率 ≥ 95%
- 脚本可一键运行：`python preprocess.py --input raw/ --output clean/`

---

### 2. 模型训练脚本 🧠
**做什么**：实现指定架构的训练代码

**典型交付物**：
- 训练脚本（PyTorch/TensorFlow）
- 训练日志 + 最佳模型权重
- README（如何训练、超参数说明）

**验收标准示例**：
- 在 XXX 数据集上准确率 ≥ 85%
- 支持 GPU 训练，显存占用 < 4GB
- `python train.py --epochs 50 --lr 0.001` 可直接运行

---

### 3. 可视化 Dashboard 📈
**做什么**：把数据或训练过程可视化

**典型交付物**：
- 可交互的 Web 页面（Streamlit/Gradio/纯 HTML）
- 关键图表截图
- 部署说明

**验收标准示例**：
- 展示训练 loss/accuracy 曲线
- 支持上传新数据实时预测
- `streamlit run app.py` 一键启动

---

### 4. API 封装 / Docker 化 🐳
**做什么**：把模型包装成可调用的服务

**典型交付物**：
- FastAPI/Flask 服务代码
- Dockerfile + docker-compose.yml
- API 文档（接口说明 + 示例请求）

**验收标准示例**：
- `docker-compose up` 一键启动
- POST /predict 接口返回正确预测
- 响应时间 < 500ms

---

### 5. 基线模型复现 📄
**做什么**：复现论文中的基线结果

**典型交付物**：
- 复现代码
- 实验结果对比表（论文 vs 复现）
- 差异分析

**验收标准示例**：
- 复现精度与论文报告差距 < 3%
- 代码有详细注释
- 提供随机种子，结果可复现

---

### 6. 单元测试 + Benchmark 🧪
**做什么**：为已有代码写测试和性能基准

**典型交付物**：
- pytest 测试套件
- Benchmark 报告（速度/内存/准确率）
- CI 配置文件（可选）

**验收标准示例**：
- 测试覆盖率 ≥ 80%
- 所有测试通过
- Benchmark 包含至少 3 组对比实验

---

## 三、如何拆解你的毕设

### Step 1：列出毕设的所有模块

```
毕设题目：基于联邦学习的医学图像分类系统

模块拆解：
├── 1. 数据收集与预处理 ← 可拆
├── 2. 本地模型训练     ← 可拆
├── 3. 联邦学习聚合算法 ← 核心部分，自己来
├── 4. 系统后端 API     ← 可拆
├── 5. 前端可视化       ← 可拆
└── 6. 实验与评估       ← 部分可拆
```

### Step 2：标记哪些可以外包

**原则**：核心创新自己做，辅助模块可以外包

- ✅ 数据预处理 — 别人做不影响你的创新点
- ✅ 本地模型训练 — 标准流程，别人能做
- ❌ 联邦学习聚合 — 这是你的核心贡献
- ✅ 系统 API — 工程活，别人擅长
- ✅ 前端可视化 — 别人可能做得更好看
- ✅ Benchmark 测试 — 别人帮你验证

### Step 3：把每个可外包模块写成任务

用这个模板：

```
标题：[具体做什么] — [技术栈]
描述：
  背景：我的毕设是 XXX，需要 YYY
  目标：实现 ZZZ
  技术要求：Python, PyTorch, ...
交付物：
  1. XXX 脚本
  2. README 文档
  3. 测试代码
验收标准：
  - 准确率 ≥ XX%
  - 代码可一键运行
  - 有完整文档
```

---

## 四、发布任务时的注意事项

### 1. 标题要具体

| ❌ 差标题 | ✅ 好标题 |
|----------|----------|
| 帮我做数据预处理 | 医学CT图像预处理Pipeline（去噪+增强+分割） |
| 训练模型 | PyTorch ResNet18 肺部X光分类器训练脚本 |
| 做个前端 | Streamlit 联邦学习训练监控面板 |

### 2. 验收标准要量化

| ❌ 模糊 | ✅ 量化 |
|--------|--------|
| "效果要好" | "测试集 F1 ≥ 0.85" |
| "代码能跑" | "`python main.py` 一键运行，无报错" |
| "速度要快" | "单张推理 < 100ms (CPU)" |

### 3. 数据要说明清楚

- 用什么数据？从哪来？
- 数据格式？大小？
- 你能提供还是需要对方自己下载？
- 有没有隐私/版权限制？

### 4. 截止时间要合理

| 难度 | 建议工时 | 建议截止 |
|------|---------|---------|
| 简单 | ≤ 4h | 3-5 天 |
| 中等 | 4-12h | 1-2 周 |
| 困难 | > 12h | 2-3 周 |

---

## 五、接单者的最佳实践

### 申请时

1. **认真写方案** — 不写方案的申请最容易被忽略
2. **展示能力** — 提到相关经验或项目
3. **确认理解** — 用自己的话复述任务要求

### 交付时

1. **代码可运行** — 提供完整的环境配置说明
2. **文档要写好** — README 至少包含：安装、运行、参数说明
3. **提前交付** — 留出修改时间，不要卡截止日期
4. **主动沟通** — 遇到问题及时联系出题者

### 信用分规则

| 行为 | 信用分变化 |
|------|-----------|
| 按时交付 | +2 |
| 逾期交付 | -2 |
| 一次通过验收 | +3 |
| 需要修改 | -1 |
| 验收不通过 | -3 |
| 好评（质量评分 4-5） | +2 |

---

## 六、常见问题

**Q：我的毕设没有 ML 部分，能发布任务吗？**
A：任务市场专注于 ML 相关任务。如果你的毕设没有 ML 模块，你可以作为接单者帮其他同学做任务。

**Q：出题者和做题者可以是同一个人吗？**
A：不可以。你不能接自己发布的任务。

**Q：任务被拒绝了怎么办？**
A：查看管理员的审核意见，修改后重新提交。

**Q：交付被要求修改怎么办？**
A：根据出题者的反馈修改，重新提交交付物。信用分会小幅扣减（-1），但比被拒绝（-3）好得多。

**Q：出题者不验收怎么办？**
A：联系助教处理。出题者也有验收义务，长期不验收会影响出题者自己的评分。

**Q：一个任务可以多人接吗？**
A：出题者只能接受一个申请。但可以拒绝后接受其他人。

---

## 七、评分占比

```
总成绩 = 出题分(40%) + 做题分(60%)

出题分：
  - 任务描述质量（边界清晰、可执行）     15%
  - 验收认真度（是否认真 review 代码）    15%
  - 出题创新性 / 实用性                  10%

做题分：
  - 代码质量 + 可运行性                  25%
  - 文档 + Demo 完整度                   15%
  - 功能完成度 / 验收通过                20%
```

---

## 八、参考资源

- **D21 AI 任务市场**：[https://zh.d21.ai/](https://zh.d21.ai/) — 类似的 AI 任务众包平台，可以参考任务发布格式和验收流程

---

*祝你在任务市场有所收获！有问题随时联系助教。*
